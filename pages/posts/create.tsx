import * as React from 'react';

export interface CreatePostPageProps {
}

export default function CreatePostPage (props: CreatePostPageProps) {
  return (
    <div>
      Create Post Page by enter /posts/create
    </div>
  );
}
